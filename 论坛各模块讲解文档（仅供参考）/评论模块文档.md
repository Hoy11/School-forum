评论模块功能文档

一、模块字段设计

1.1 评论表(Comment)字段设计
id：整型，主键，评论ID，自增
post_id：整型，外键，关联帖子表，评论所属帖子ID
user_id：整型，外键，关联用户表，评论发布用户ID
content：文本型，评论内容
create_time：日期时间型，评论发布时间
parent_id：整型，外键，关联评论表，父评论ID(用于回复功能)
status：整型，评论状态(0-删除，1-正常)

1.2 评论点赞表(CommentLike)字段设计
id：整型，主键，记录ID，自增
comment_id：整型，外键，关联评论表，被点赞的评论ID
user_id：整型，外键，关联用户表，点赞用户ID
create_time：日期时间型，点赞时间
status：整型，点赞状态(0-取消点赞，1-已点赞)

1.3 实体类定义

【Comment.java】
实体类包含评论表的基本字段，以及以下扩展字段：
username：字符串，评论用户名
userAvatar：字符串，评论用户头像
postTitle：字符串，帖子标题
parentUsername：字符串，父评论用户名
likeCount：整型，点赞数量
isLiked：布尔型，当前用户是否点赞
user：User对象，评论用户信息

【CommentLike.java】
实体类包含评论点赞表的基本字段，无扩展字段

1.4 数据传输对象定义

【CommentDTO.java】
postId：Long类型，帖子ID，必填
content：String类型，评论内容，必填
parentId：Long类型，父评论ID，可选
status：Integer类型，评论状态，可选

【CommentTreeDTO.java】
包含Comment实体类的所有字段，及以下扩展字段：
children：List<CommentTreeDTO>类型，子评论列表

二、评论发布功能

2.1 功能概述
评论发布功能允许用户在帖子详情页发表评论或回复其他用户的评论，形成树状结构的评论体系。系统支持两级评论，即评论与回复。

2.2 实现流程

2.2.1 前端实现流程
1) 用户在帖子详情页底部评论框输入评论内容
2) 点击"发表评论"按钮提交
3) 若要回复他人评论，点击"回复"按钮，在评论下方出现回复框
4) 输入回复内容并提交

【PostDetail.vue：评论提交函数】
函数首先验证用户是否登录及内容是否为空，然后构造评论数据提交到服务器:
const submitComment = async () => {
  if (!userStore.isLoggedIn) {
    ElMessage.warning('请先登录')
    return
  }
  if (!commentForm.content.trim()) {
    ElMessage.warning('请输入评论内容')
    return
  }
  submitting.value = true
  try {
    const data = {
      postId: Number(route.params.id),
      content: commentForm.content.trim(),
      parentId: null  // 主评论的 parentId 为 null
    }
    await request.post('/comment/add', data, {
      successMsg: '评论发表成功',
      onSuccess: () => {
        commentForm.content = ''
        fetchCommentTree() // 重新获取评论树
      }
    })
  } catch (error) {
    console.error('发表评论失败:', error)
  } finally {
    submitting.value = false
  }
}

2.2.2 后端实现流程
1) 接收评论数据，验证用户身份及评论合法性
2) 对于回复类评论，验证父评论是否存在
3) 限制评论层级最多为2级
4) 保存评论数据到数据库

【CommentService.java：创建评论方法】
方法验证用户和帖子存在性，处理回复评论的父子关系，并保存评论到数据库:
public void createComment(CommentDTO commentDTO, Long userId) {
    // 验证用户是否存在
    User user = userService.getUserById(userId);
    
    // 验证帖子是否存在
    Post post = postService.getPostById(commentDTO.getPostId());
    
    // 如果是回复评论，验证父评论是否存在
    if (commentDTO.getParentId() != null) {
        Comment parentComment = getCommentById(commentDTO.getParentId());
        
        // 验证父评论和子评论是否属于同一帖子
        if (!parentComment.getPostId().equals(commentDTO.getPostId())) {
            throw new ServiceException("回复的评论不属于同一帖子");
        }
        
        // 限制评论最多为2级
        if (parentComment.getParentId() != null) {
            // 如果父评论已经是子评论，则将新评论的父ID设置为顶级评论ID
            commentDTO.setParentId(parentComment.getParentId());
        }
    }
    
    // 创建评论对象
    Comment comment = new Comment();
    comment.setUserId(userId);
    comment.setPostId(commentDTO.getPostId());
    comment.setContent(commentDTO.getContent());
    comment.setCreateTime(new Date());
    comment.setParentId(commentDTO.getParentId());
    comment.setStatus(commentDTO.getStatus() != null ? commentDTO.getStatus() : 1);
    
    if (commentMapper.insert(comment) <= 0) {
        throw new ServiceException("评论创建失败");
    }
}

2.3 权限控制
- 所有已登录用户可发表评论及回复
- 未登录用户无法评论，点击评论按钮会提示登录
- 用户只能删除自己发表的评论

2.4 异常处理
- 评论内容为空：前端验证提示"请输入评论内容"
- 用户未登录：提示"请先登录"
- 父评论不存在：抛出"回复的评论不存在"异常
- 父评论与帖子不匹配：抛出"回复的评论不属于同一帖子"异常

三、评论显示功能

3.1 功能概述
评论显示功能将帖子下的所有评论以树状结构展示，包括评论内容、评论用户、评论时间、点赞数等信息，并支持评论分页加载。

3.2 实现流程

3.2.1 前端实现流程
1) 帖子详情页加载时，获取评论树数据
2) 递归渲染评论树，显示主评论及其回复
3) 为每个评论显示点赞数和当前用户的点赞状态

【PostDetail.vue：获取评论树函数】
函数从服务器获取带有点赞信息的评论树结构，并计算总评论数:
const fetchCommentTree = async () => {
  try {
    await request.get('/comment/tree/' + route.params.id, null, {
      showDefaultMsg: false,
      onSuccess: (res) => {
        commentTree.value = res
          
        // 计算评论总数（递归计算所有子评论）
        const countComments = (comments) => {
          let count = comments.length
          for (const comment of comments) {
            if (comment.children && comment.children.length > 0) {
              count += countComments(comment.children)
            }
          }
          return count
        }
        
        totalComments.value = countComments(commentTree.value)
        post.value.commentCount = totalComments.value
      }
    })
  } catch (error) {
    console.error('获取评论树失败:', error)
    ElMessage.error('获取评论树失败')
  }
}

3.2.2 后端实现流程
1) 获取帖子的所有评论记录
2) 为每条评论添加点赞信息
3) 构建评论树结构
4) 返回树状评论数据

【CommentService.java：构建评论树方法】
方法获取帖子所有评论，添加点赞信息，构建树状结构并返回:
public List<CommentTreeDTO> getCommentTreeByPostIdWithLikeInfo(Long postId, Long userId) {
    // 获取帖子所有评论
    List<Comment> allComments = getCommentsByPostId(postId);
    if (allComments.isEmpty()) {
        return new ArrayList<>();
    }
    
    // 为每个评论添加点赞信息
    for (Comment comment : allComments) {
        int likeCount = commentLikeService.getLikeCount(comment.getId());
        comment.setLikeCount(likeCount);
        
        if (userId != null) {
            boolean isLiked = commentLikeService.isLiked(comment.getId(), userId);
            comment.setIsLiked(isLiked);
        }
    }
    allComments.forEach(this::fillUserInfo);
    
    // 构建评论树
    Map<Long, List<Comment>> commentsByParentId = allComments.stream()
            .collect(Collectors.groupingBy(
                    comment -> comment.getParentId() == null ? 0L : comment.getParentId()
            ));
    
    // 获取顶级评论
    List<Comment> rootComments = commentsByParentId.getOrDefault(0L, new ArrayList<>());
    rootComments.forEach(this::fillUserInfo);
    // 转换为树形结构
    return rootComments.stream().map(comment -> {
        CommentTreeDTO node = new CommentTreeDTO();
        // 填充评论节点数据...
        
        // 递归构建子评论
        node.setChildren(buildChildrenTree(comment.getId(), commentsByParentId));
        
        return node;
    }).collect(Collectors.toList());
}

3.3 评论组件设计

【CommentTreeItem.vue：评论树组件】
组件递归渲染评论树，显示评论内容、用户信息、发布时间等，并支持回复、点赞等操作:
<template>
  <div class="comment-item" :class="{'child-comment': isChild}">
    <!-- 评论头部信息 -->
    <div class="comment-header">
      <el-avatar :size="32" :src="'/api'+comment?.user?.avatar" />
      <div class="comment-info">
        <span class="username">{{ comment.username }}</span>
        <span class="comment-time">{{ formatDate(comment.createTime) }}</span>
      </div>
      <!-- 操作按钮 -->
    </div>

    <!-- 评论内容 -->
    <div class="comment-content">
      <!-- 回复标记 -->
      {{ comment.content }}
    </div>

    <!-- 评论操作区域 -->
    <div class="comment-actions">
      <!-- 回复和点赞按钮 -->
    </div>

    <!-- 回复表单 -->
    <!-- 子评论列表 - 递归调用 -->
    <div class="children-comments" v-if="comment.children && comment.children.length > 0">
      <comment-tree-item
        v-for="child in comment.children"
        :key="child.id"
        :comment="child"
        :is-child="true"
        @reply="handleChildReply"
        @submit-reply="handleChildSubmitReply"
        @delete="handleChildDelete"
        @like-updated="handleChildLikeUpdated"
      />
    </div>
  </div>
</template>

四、评论点赞功能

4.1 功能概述
评论点赞功能允许用户对评论进行点赞或取消点赞操作，系统记录每个评论的点赞数并显示在评论旁边。

4.2 实现流程

4.2.1 前端实现流程
1) 用户点击评论下方的点赞按钮
2) 点赞状态切换，点赞数相应增减
3) 点赞图标根据状态变化显示不同样式

【CommentTreeItem.vue：点赞处理函数】
函数处理点赞操作，包括验证登录状态，发送点赞请求和更新UI:
const handleLike = async () => {
  if (!userStore.isLoggedIn) {
    ElMessage.warning('请先登录')
    return
  }
  
  try {
    await request.post(`/comment/like/${props.comment.id}`, null, {
      showDefaultMsg: false,
      onSuccess: (isLiked) => {
        // 通过emit更新评论数据
        emit('update:comment', {
          ...props.comment,
          isLiked: isLiked,
          likeCount: (props.comment.likeCount || 0) + (isLiked ? 1 : -1)
        })
        
        // 触发事件通知父组件
        emit('like-updated', { 
          commentId: props.comment.id, 
          isLiked: isLiked 
        })
        
        ElMessage.success(isLiked ? '点赞成功' : '取消点赞成功')
      }
    })
  } catch (error) {
    console.error('点赞操作失败:', error)
    ElMessage.error('点赞操作失败')
  }
}

4.2.2 后端实现流程
1) 验证用户和评论存在性
2) 查询用户是否已点赞该评论
3) 根据查询结果执行点赞或取消点赞操作
4) 返回最新点赞状态

【CommentLikeService.java：点赞切换方法】
方法检查点赞记录，执行点赞或取消点赞操作并返回新状态:
@Transactional
public boolean toggleLike(Long commentId, Long userId) {
    // 验证用户是否存在
    User user = userService.getUserById(userId);
    
    // 验证评论是否存在
    Comment comment = commentService.getCommentById(commentId);
    
    // 查询点赞记录
    CommentLike like = commentLikeMapper.selectLikeByCommentIdAndUserId(commentId, userId);
    
    boolean isNowLiked;
    if (like == null) {
        // 不存在点赞记录，创建新记录
        like = new CommentLike();
        like.setCommentId(commentId);
        like.setUserId(userId);
        like.setCreateTime(new Date());
        like.setStatus(1); // 已点赞
        
        if (commentLikeMapper.insert(like) <= 0) {
            throw new ServiceException("点赞操作失败");
        }
        isNowLiked = true;
    } else {
        // 存在记录，切换状态
        int newStatus = like.getStatus() == 1 ? 0 : 1;
        like.setStatus(newStatus);
        like.setCreateTime(new Date()); // 更新时间
        
        if (commentLikeMapper.updateById(like) <= 0) {
            throw new ServiceException("更新点赞状态失败");
        }
        isNowLiked = newStatus == 1;
    }
    
    return isNowLiked;
}

4.3 点赞计数实现

【CommentLikeMapper.java：点赞数量查询】
接口定义了查询评论点赞数量的方法:
Integer countLikesByCommentId(@Param("commentId") Long commentId);

【CommentLikeService.java：获取点赞数】
方法获取评论的点赞数量:
public int getLikeCount(Long commentId) {
    Integer count = commentLikeMapper.countLikesByCommentId(commentId);
    return count != null ? count : 0;
}

五、评论管理功能

5.1 功能概述
评论管理功能提供后台管理评论的能力，包括评论查询、编辑、删除和状态管理等操作，管理员可通过此功能维护论坛评论内容的健康。

5.2 实现流程

5.2.1 前端实现流程
1) 管理员在后台评论管理页面查看所有评论
2) 支持多条件查询筛选评论
3) 可对评论进行编辑、删除、状态修改等操作

【CommentManagement.vue：评论管理界面】
组件实现评论管理功能，包括条件搜索、分页显示和批量操作:
<template>
  <div class="comment-management-container">
    <!-- 搜索卡片 -->
    <el-card class="search-card">
      <div class="search-form">
        <el-form :model="searchForm" inline>
          <!-- 搜索条件表单 -->
        </el-form>
      </div>
    </el-card>

    <!-- 操作栏 -->
    <div class="operation-bar">
      <!-- 批量操作按钮 -->
    </div>

    <!-- 数据表格 -->
    <el-card class="table-card">
      <el-table
        v-loading="tableLoading"
        :data="commentList"
        border
        stripe
        style="width: 100%"
        @selection-change="handleSelectionChange"
      >
        <!-- 表格列定义 -->
      </el-table>

      <!-- 分页 -->
      <div class="pagination-container">
        <!-- 分页组件 -->
      </div>
    </el-card>

    <!-- 编辑评论对话框 -->
    <el-dialog
      v-model="commentFormVisible"
      :title="formOperation === 'edit' ? '编辑评论' : '新增评论'"
      width="500px"
    >
      <!-- 编辑表单 -->
    </el-dialog>
  </div>
</template>

5.2.2 后端实现流程
1) 提供分页查询接口，支持多条件筛选
2) 提供评论更新、删除、状态修改API
3) 支持批量操作功能

【CommentController.java：分页查询方法】
方法接收多条件查询参数，返回分页结果:
@GetMapping("/page")
@Operation(summary = "分页查询评论", description = "分页查询评论，支持多条件筛选")
public Result getCommentsByPage(
        @RequestParam(required = false) String content,
        @RequestParam(required = false) String username,
        @RequestParam(required = false) String postTitle,
        @RequestParam(required = false) Integer postId,
        @RequestParam(required = false) Integer status,
        @RequestParam(defaultValue = "1") Integer current,
        @RequestParam(defaultValue = "10") Integer size) {
    Page<Comment> page = commentService.getCommentsByPage(content, username, postTitle, postId,status, current, size);
    return Result.success(page);
}

5.3 批量操作实现

【CommentManagement.vue：批量删除函数】
函数实现批量删除评论的功能:
const handleBatchDelete = () => {
  if (selectedComments.value.length === 0) {
    ElMessage.warning('请至少选择一条评论')
    return
  }
  
  ElMessageBox.confirm(`确定要删除选中的 ${selectedComments.value.length} 条评论吗？此操作不可恢复！`, '警告', {
    confirmButtonText: '确定',
    cancelButtonText: '取消',
    type: 'warning'
  }).then(async () => {
    try {
      const ids = selectedComments.value.map(item => item.id)
      const response = await request.delete('/comment/batch?ids='+ids,null,{
        successMsg: '批量删除成功',
        onSuccess: (res) => {
          fetchCommentList()
        }
      })
    } catch (error) {
      console.error('批量删除评论失败:', error)
      ElMessage.error('批量删除评论失败')
    }
  }).catch(() => {})
}

【CommentController.java：批量删除接口】
接口处理批量删除请求:
@DeleteMapping("/batch")
@Operation(summary = "批量删除评论", description = "批量删除评论")
public Result deleteBatch(@RequestParam List<Integer> ids) {
    commentService.deleteBatch(ids);
    return Result.success("批量删除成功");
}

六、用户评论交互功能

6.1 功能概述
用户评论交互功能允许用户对自己发表的评论进行管理，包括删除自己的评论、查看评论历史等操作。同时，用户可以对他人评论进行点赞和举报。

6.2 评论删除功能

【CommentTreeItem.vue：评论删除函数】
函数处理用户删除自己评论的逻辑:
const handleDelete = () => {
  if (!isCurrentUserComment.value) {
    return
  }
  
  ElMessageBox.confirm('确定要删除这条评论吗？此操作不可恢复！', '提示', {
    confirmButtonText: '确定',
    cancelButtonText: '取消',
    type: 'warning'
  }).then(async () => {
    try {
      await request.delete(`/comment/user/${props.comment.id}`, {
        successMsg: '评论删除成功',
        onSuccess: () => {
          emit('delete', props.comment.id)
        }
      })
    } catch (error) {
      console.error('删除评论失败:', error)
    }
  }).catch(() => {})
}

【CommentController.java：用户删除评论接口】
接口处理用户删除自己评论的请求:
@DeleteMapping("/user/{id}")
@Operation(summary = "用户删除自己的评论", description = "用户删除自己的评论")
public Result deleteUserComment(@PathVariable Long id, HttpServletRequest request) {
    // 从请求头中获取token并解析用户ID
    String token = request.getHeader("token");
    if (StringUtils.isBlank(token)) {
        token = request.getParameter("token");
    }
    if (StringUtils.isBlank(token)) {
        return Result.error("未授权，请先登录");
    }
    
    Long userId;
    try {
        String userIdStr = JWT.decode(token).getAudience().get(0);
        userId = Long.valueOf(userIdStr);
    } catch (Exception e) {
        return Result.error("token无效或已过期");
    }
    
    // 调用服务删除用户自己的评论
    boolean success = commentService.deleteUserComment(id, userId);
    if (success) {
        return Result.success("评论删除成功");
    } else {
        return Result.error("您只能删除自己的评论");
    }
}

6.3 评论举报功能

【CommentTreeItem.vue：评论举报函数】
函数处理用户举报评论的逻辑:
const handleReport = () => {
  if (!userStore.isLoggedIn) {
    ElMessage.warning('请先登录')
    return
  }
  reportDialogVisible.value = true
}

七、异常处理机制

7.1 前端异常处理
1) 使用try-catch捕获异常，并在console中记录错误
2) 使用ElMessage显示友好的错误提示
3) 网络请求错误统一处理

7.2 后端异常处理
1) 使用自定义ServiceException处理业务异常
2) 统一Result响应格式，规范错误码和错误信息
3) 关键操作使用事务保证数据一致性

【CommentService.java：异常处理示例】
方法中使用异常处理确保操作的完整性:
@Transactional
public boolean toggleLike(Long commentId, Long userId) {
    try {
        // 验证操作...
        
        // 数据库操作...
        
        return isNowLiked;
    } catch (Exception e) {
        throw new ServiceException("点赞操作异常: " + e.getMessage());
    }
}

八、与其他模块的交互

8.1 与用户模块交互
1) 获取评论用户信息：查询用户表获取用户名、头像等信息
2) 权限验证：验证用户登录状态和操作权限

8.2 与帖子模块交互
1) 关联帖子：每条评论关联到特定帖子
2) 评论计数：帖子详情页显示评论总数
3) 帖子活跃度：评论数量影响帖子排序和热度

九、性能优化措施

9.1 数据库优化
1) 评论表建立索引：post_id, user_id, parent_id, status
2) 评论点赞表建立联合索引：comment_id + user_id

9.2 查询优化
1) 评论树构建使用Map分组，减少循环查询
2) 分页查询限制每页数量，避免大量数据查询

9.3 前端优化
1) 懒加载评论组件，减少初始加载时间
2) 使用虚拟滚动处理大量评论
3) 本地缓存点赞状态，减少服务器请求

十、总结

评论模块是论坛系统的核心交互功能，实现了评论发布、回复、点赞、管理等完整功能链，采用树状结构组织评论数据，提供了良好的用户交互体验。模块与用户模块、帖子模块紧密集成，通过权限控制和异常处理保证了系统的安全性和稳定性。 