资源模块功能文档

一、模块字段设计

1.1 资源表(Resource)字段设计
id：整型，主键，资源ID，自增
resource_name：字符串型，资源名称
description：字符串型，资源描述
file_path：字符串型，文件路径
file_size：整型，文件大小(KB)
file_type：字符串型，文件类型
user_id：整型，外键，关联用户表，上传用户ID
category_id：整型，外键，关联资源分类表，分类ID
download_count：整型，下载次数
create_time：日期时间型，上传时间
status：整型，资源状态(0-删除，1-正常)

1.2 资源分类表(ResourceCategory)字段设计
id：整型，主键，分类ID，自增
category_name：字符串型，分类名称
description：字符串型，分类描述
create_time：日期时间型，创建时间
status：整型，分类状态(0-禁用，1-正常)

1.3 实体类定义

【ForumResource.java】
实体类包含资源表的基本字段，以及以下扩展字段：
uploadUsername：字符串，上传者用户名（非数据库字段）
categoryName：字符串，分类名称（非数据库字段）

【ResourceCategory.java】
实体类包含资源分类表的基本字段，无扩展字段

1.4 数据传输对象定义

【ResourceDTO.java】
resourceName：String类型，资源名称，必填
description：String类型，资源描述，可选
categoryId：Long类型，分类ID，必填
filePath：String类型，文件路径，可选
fileSize：Integer类型，文件大小，可选
fileType：String类型，文件类型，可选
status：Integer类型，资源状态，可选

【ResourceCategoryDTO.java】
categoryName：String类型，分类名称，必填
description：String类型，分类描述，可选
status：Integer类型，分类状态，可选

二、资源管理功能

2.1 功能概述
资源管理功能允许管理员或教师上传、编辑、删除资源文件，包括文档、图片、视频等多种类型，并对资源进行分类管理和状态控制。用户可以浏览、搜索和下载系统中的资源。

2.2 资源上传功能

2.2.1 前端实现流程
1) 用户点击"上传资源"按钮，打开上传表单
2) 填写资源名称、选择分类、添加描述等信息
3) 选择要上传的文件
4) 点击提交按钮上传

【Resources.vue：文件上传实现】
文件上传处理函数实现了文件选择和表单提交逻辑:
const handleFileChange = (file) => {
  // 文件大小检查
  const isLt50M = file.size / 1024 / 1024 < 50
  
  if (!isLt50M) {
    ElMessage.error('上传文件大小不能超过50MB!')
    return
  }
  
  selectedFile.value = file.raw
  
  // 获取文件扩展名
  const fileExt = file.name.substring(file.name.lastIndexOf('.') + 1).toLowerCase()
  
  // 设置资源文件名（如果用户还没有输入）
  if (!uploadForm.resourceName) {
    uploadForm.resourceName = file.name.substring(0, file.name.lastIndexOf('.'))
  }
}

const submitUpload = async () => {
  // 表单验证
  if (!uploadFormRef.value) return
  
  const valid = await uploadFormRef.value.validate().catch(() => false)
  if (!valid) {
    ElMessage.warning('请完善表单信息')
    return
  }
  
  if (!selectedFile.value && !uploadForm.filePath) {
    ElMessage.warning('请选择要上传的文件')
    return
  }
  
  if (!uploadForm.filePath) {
    // 如果还没有上传文件，先上传
    try {
      const uploadSuccess = await uploadFile()
      if (!uploadSuccess) return
    } catch (error) {
      return
    }
  }
  
  try {
    uploading.value = true
    await request.post('/resource/add', uploadForm, {
      successMsg: '资源上传成功',
      onSuccess: () => {
        uploadDialogVisible.value = false
        getResourceList()
        // 重置表单和文件
        handleDialogClosed()
      }
    })
  } catch (error) {
    console.error('资源上传失败:', error)
  } finally {
    uploading.value = false
  }
}

2.2.2 后端实现流程
1) 验证用户身份和权限
2) 接收文件并存储到指定位置
3) 将资源信息保存到数据库

【ResourceService.java：创建资源方法】
方法验证用户和分类存在性，并创建资源记录:
public void createResource(ResourceDTO resourceDTO, Long userId) {
    // 验证用户是否存在
    userService.getUserById(userId);
    
    // 验证分类是否存在
    ResourceCategory category = categoryMapper.selectById(resourceDTO.getCategoryId());
    if (category == null) {
        throw new ServiceException("资源分类不存在");
    }
    
    // 创建资源对象
    ForumResource resource = new ForumResource();
    resource.setResourceName(resourceDTO.getResourceName());
    resource.setDescription(resourceDTO.getDescription());
    resource.setCategoryId(resourceDTO.getCategoryId());
    resource.setFilePath(resourceDTO.getFilePath());
    resource.setFileSize(resourceDTO.getFileSize());
    resource.setFileType(resourceDTO.getFileType());
    resource.setUserId(userId);
    resource.setCreateTime(new Date());
    resource.setDownloadCount(0);
    resource.setStatus(resourceDTO.getStatus() != null ? resourceDTO.getStatus() : 1);
    
    // 插入数据库
    if (resourceMapper.insert(resource) <= 0) {
        throw new ServiceException("资源创建失败");
    }
}

2.3 资源列表与筛选功能

2.3.1 前端实现流程
1) 页面加载时获取资源列表和分类列表
2) 提供搜索框和筛选选项（按名称、分类、文件类型筛选）
3) 以卡片形式展示资源列表
4) 支持分页加载更多资源

【Resources.vue：资源筛选实现】
资源筛选和获取函数实现了条件查询和分页功能:
const getResourceList = async () => {
  const params = {
    ...queryParams,
    status: 1
  }
  try {
    const res = await request.get('/resource/page', params, {
      showDefaultMsg:false,
      onSuccess:(data)=>{
        resourceList.value = (data.records || []).map(item => ({
          ...item,
          downloadUrl: item.filePath && !item.filePath.startsWith('/api') ? 
            `/api${item.filePath}` : item.filePath
        }))
        total.value = data.total || 0
      }
    })
  } catch (error) {
    console.error('获取资源列表失败:', error)
  }
}

const handleSearch = () => {
  queryParams.currentPage = 1
  getResourceList()
}

2.3.2 后端实现流程
1) 接收查询参数（名称、分类、文件类型等）
2) 构建查询条件
3) 执行分页查询
4) 返回结果集

【ResourceService.java：分页查询方法】
方法实现了多条件分页查询:
public Page<ForumResource> getResourcesByPage(String resourceName, Long categoryId, String fileType, Long userId, Integer status, Integer currentPage, Integer size) {
    Page<ForumResource> page = new Page<>(currentPage, size);

    Page<ForumResource> forumResourcePage = resourceMapper.selectResourcePage(page, resourceName, categoryId, fileType, userId, status);
    forumResourcePage.getRecords().forEach(this::fillRelateInfo);

    return forumResourcePage;
}

2.4 资源下载功能

2.4.1 前端实现流程
1) 用户点击资源卡片进入详情页
2) 点击"下载资源"按钮
3) 系统验证用户权限
4) 开始下载文件并增加下载计数

【ResourceDetail.vue：下载处理函数】
下载函数实现了文件下载和下载计数增加逻辑:
const handleDownload = async () => {
  if (!userStore.isLoggedIn) {
    ElMessage.warning('请先登录后下载资源')
    return
  }

  try {
    downloading.value = true
    // 增加下载次数
    await request.put(`/resource/${resource.value.id}/download`)
    
    // 构建完整下载URL
    let downloadUrl = resource.value.filePath;
    
    // 检查URL是否已经包含了api前缀
    if (!downloadUrl.startsWith('/api') && !downloadUrl.startsWith('http')) {
      downloadUrl = `/api${downloadUrl}`;
    }
    
    // 使用fetch API进行下载，添加token
    const response = await fetch(downloadUrl, {
      method: 'GET',
      headers: {
        'token': userStore.token
      }
    });
    
    if (!response.ok) {
      throw new Error(`HTTP error! status: ${response.status}`);
    }
    
    // 获取文件名
    const filename = resource.value.resourceName + '.' + resource.value.fileType;
    
    // 获取文件blob
    const blob = await response.blob();
    const url = window.URL.createObjectURL(blob);
    
    // 创建下载链接并模拟点击
    const a = document.createElement('a');
    a.href = url;
    a.download = filename;
    document.body.appendChild(a);
    a.click();
    
    // 清理
    window.URL.revokeObjectURL(url);
    document.body.removeChild(a);
    
    ElMessage.success('下载成功')
  } catch (error) {
    console.error('下载失败:', error);
    ElMessage.error('下载失败，请稍后重试')
  } finally {
    downloading.value = false
  }
}

2.4.2 后端实现流程
1) 接收下载请求
2) 验证资源存在性
3) 增加资源下载计数
4) 返回文件流

【ResourceService.java：下载计数方法】
方法实现了资源下载计数增加功能:
public void incrementDownloadCount(Long id) {
    // 验证资源是否存在
    getResourceById(id);
    
    if (resourceMapper.updateDownloadCount(id) <= 0) {
        throw new ServiceException("下载次数更新失败");
    }
}

三、资源分类管理功能

3.1 功能概述
资源分类管理功能允许管理员创建、编辑和删除资源分类，用于对资源进行分类管理，提高资源的可查找性和组织性。

3.2 分类创建功能

3.2.1 前端实现流程
1) 管理员点击"新增分类"按钮，打开分类表单
2) 填写分类名称和描述信息
3) 选择分类状态（启用/禁用）
4) 点击确定按钮提交

【ResourceCategoryManagement.vue：分类创建实现】
分类创建函数实现了表单提交逻辑:
const handleAdd = () => {
  dialogTitle.value = '新增分类'
  categoryForm.id = ''
  categoryForm.categoryName = ''
  categoryForm.description = ''
  categoryForm.status = 1
  dialogVisible.value = true
}

const handleSubmit = async () => {
  if (!categoryFormRef.value) return
  
  try {
    await categoryFormRef.value.validate()
    
    if (dialogTitle.value === '新增分类') {
      // 创建分类
      const categoryData = {
        categoryName: categoryForm.categoryName,
        description: categoryForm.description,
        status: categoryForm.status
      }
      
      request.post('/resource/category/add', categoryData, {
        successMsg: '分类创建成功',
        onSuccess: () => {
          dialogVisible.value = false
          fetchCategories()
        }
      })
    } else {
      // 编辑分类逻辑...
    }
  } catch (error) {
    console.error('表单验证失败:', error)
  }
}

3.2.2 后端实现流程
1) 接收分类信息
2) 验证分类名称唯一性
3) 创建分类记录

【ResourceCategoryService.java：创建分类方法】
方法实现了分类名称验证和创建逻辑:
public void createCategory(ResourceCategoryDTO categoryDTO) {
    // 检查分类名称是否已存在
    ResourceCategory existingCategory = categoryMapper.selectOne(
            new LambdaQueryWrapper<ResourceCategory>()
                    .eq(ResourceCategory::getCategoryName, categoryDTO.getCategoryName())
    );
    if (existingCategory != null) {
        throw new ServiceException("分类名称已存在");
    }
    
    // 创建分类对象
    ResourceCategory category = new ResourceCategory();
    category.setCategoryName(categoryDTO.getCategoryName());
    category.setDescription(categoryDTO.getDescription());
    category.setCreateTime(new Date());
    category.setStatus(categoryDTO.getStatus() != null ? categoryDTO.getStatus() : 1);
    
    // 插入数据库
    if (categoryMapper.insert(category) <= 0) {
        throw new ServiceException("分类创建失败");
    }
}

3.3 分类列表与管理功能

3.3.1 前端实现流程
1) 页面加载时获取分类列表
2) 提供搜索框和筛选选项（按名称、状态筛选）
3) 以表格形式展示分类列表
4) 支持编辑、删除、状态切换等操作

【ResourceCategoryManagement.vue：分类管理实现】
分类列表加载和管理函数实现了查询和操作功能:
const fetchCategories = () => {
  tableLoading.value = true
  const params = {
    categoryName: searchForm.categoryName || undefined,
    status: searchForm.status === '' ? undefined : searchForm.status,
    currentPage: currentPage.value,
    size: pageSize.value
  }
  
  request.get('/resource/category/page', params, {
    showDefaultMsg: false,
    onSuccess: (res) => {
      tableData.value = res.records
      total.value = res.total
      tableLoading.value = false
    },
    onError: (error) => {
      console.error('获取资源分类列表失败:', error)
      tableLoading.value = false
    }
  })
}

const handleStatusChange = (id, status) => {
  request.put(`/resource/category/${id}/status?status=${status}`, null, {
    successMsg: `状态已${status === 1 ? '启用' : '禁用'}`,
    onSuccess: () => {
      fetchCategories()
    },
    onError: () => {
      fetchCategories() // 更新失败时刷新列表
    }
  })
}

3.3.2 后端实现流程
1) 接收查询参数（名称、状态等）
2) 构建查询条件
3) 执行分页查询
4) 返回结果集

【ResourceCategoryService.java：分页查询方法】
方法实现了分类的分页查询:
public Page<ResourceCategory> getCategoriesByPage(String categoryName, Integer status, Integer currentPage, Integer size) {
    Page<ResourceCategory> page = new Page<>(currentPage, size);
    return categoryMapper.selectCategoryPage(page, categoryName, status);
}

四、前端资源展示功能

4.1 功能概述
前端资源展示功能面向普通用户，提供资源浏览、搜索、筛选和下载功能，采用卡片式布局，展示资源基本信息，点击卡片可查看资源详情。

4.2 资源列表页

4.2.1 实现流程
1) 页面加载时获取资源列表和分类数据
2) 提供搜索框和筛选条件（名称、分类、文件类型）
3) 以卡片形式展示资源，包含资源名称、上传者、文件类型、大小等信息
4) 支持分页加载更多资源

【Resources.vue：资源列表实现】
资源卡片样式与布局实现了良好的用户体验:
<div class="resource-list">
  <el-row :gutter="20">
    <el-col :xs="24" :sm="12" :md="8" :lg="6" v-for="item in resourceList" :key="item.id">
      <div class="resource-card" @click="goToDetail(item.id)">
        <div class="resource-type">
          <el-icon v-if="item.fileType.includes('doc')"><Document /></el-icon>
          <el-icon v-else-if="item.fileType.includes('pdf')"><Tickets /></el-icon>
          <!-- 其他文件类型图标 -->
        </div>
        <div class="resource-info">
          <h3 class="resource-name">{{ item.resourceName }}</h3>
          <p class="resource-category">分类：{{ item.categoryName }}</p>
          <p class="resource-uploader">上传者：{{ item.uploadUsername }}</p>
          <p class="resource-time">{{ formatDate(item.createTime) }}</p>
          <div class="resource-meta">
            <span class="size">{{ formatFileSize(item.fileSize * 1024) }}</span>
            <span class="downloads">
              <el-icon><Download /></el-icon>
              {{ item.downloadCount }}
            </span>
          </div>
        </div>
      </div>
    </el-col>
  </el-row>
</div>

4.3 资源详情页

4.3.1 实现流程
1) 点击资源卡片跳转到详情页
2) 获取并显示资源详细信息
3) 提供下载按钮和资源详情
4) 展示相关资源推荐

【ResourceDetail.vue：资源详情实现】
资源详情页展示了完整的资源信息和操作选项:
<div class="detail-card">
  <el-card>
    <template #header>
      <div class="card-header">
        <h2 class="resource-title">
          {{ resource.resourceName }}
          <el-tag size="small" type="success" class="category-tag">
            {{ resource.categoryName }}
          </el-tag>
        </h2>
      </div>
    </template>

    <div class="resource-info">
      <!-- 资源基本信息 -->
      <div class="info-section">
        <!-- 信息项展示 -->
      </div>

      <!-- 资源描述 -->
      <div class="description-section">
        <h3>资源描述</h3>
        <p>{{ resource.description || '暂无描述' }}</p>
      </div>

      <!-- 下载按钮 -->
      <div class="action-section">
        <el-button type="primary" size="large" @click="handleDownload" :loading="downloading">
          <el-icon><Download /></el-icon> 下载资源
        </el-button>
        <el-button type="danger" size="large" @click="handleReport">
          <el-icon><Warning /></el-icon> 举报资源
        </el-button>
      </div>
    </div>
  </el-card>
</div>

五、后台管理功能

5.1 功能概述
后台管理功能面向管理员，提供资源和分类的全面管理能力，包括资源审核、编辑、删除、状态控制等功能，以及分类的创建、编辑、删除和状态管理。

5.2 资源管理页面

5.2.1 实现流程
1) 页面加载时获取资源列表和分类数据
2) 提供搜索框和筛选条件（名称、分类、上传者、状态）
3) 以表格形式展示资源列表，包含各类信息和操作按钮
4) 支持编辑、删除、状态切换、批量操作等功能

【ResourceManagement.vue：管理界面实现】
资源管理界面实现了完整的管理功能:
<div class="resource-management-container">
  <!-- 搜索卡片 -->
  <el-card class="search-card">
    <div class="search-form">
      <el-form :model="searchForm" inline>
        <!-- 搜索表单 -->
      </el-form>
    </div>
  </el-card>

  <!-- 操作栏 -->
  <div class="operation-bar">
    <!-- 操作按钮 -->
  </div>

  <!-- 数据表格 -->
  <el-card class="table-card">
    <el-table
      v-loading="tableLoading"
      :data="tableData"
      border
      stripe
      style="width: 100%"
      @selection-change="handleSelectionChange"
    >
      <!-- 表格列定义 -->
    </el-table>

    <!-- 分页 -->
    <div class="pagination-container">
      <!-- 分页组件 -->
    </div>
  </el-card>

  <!-- 资源表单对话框 -->
  <el-dialog 
    v-model="dialogVisible" 
    :title="dialogTitle" 
    width="600px" 
    :close-on-click-modal="false"
    :before-close="handleDialogClose"
  >
    <!-- 资源表单 -->
  </el-dialog>
</div>

5.3 批量操作功能

【ResourceManagement.vue：批量删除函数】
批量删除函数实现了多选删除功能:
const handleBatchDelete = () => {
  if (selectedRows.value.length === 0) return
  
  ElMessageBox.confirm(`确认批量删除选中的 ${selectedRows.value.length} 条资源记录吗？`, '提示', {
    confirmButtonText: '确认',
    cancelButtonText: '取消',
    type: 'warning'
  }).then(() => {
    const ids = selectedRows.value.map(item => item.id)
    request.delete('/resource/batch', { ids }, {
      successMsg: '批量删除成功',
      onSuccess: () => {
        fetchResources()
      }
    })
  }).catch(() => {})
}

六、异常处理机制

6.1 前端异常处理
1) 使用try-catch捕获异常，并在console中记录错误
2) 使用ElMessage显示友好的错误提示
3) 文件上传前进行类型和大小验证
4) 下载失败时提供友好提示

【Resources.vue：文件验证示例】
文件验证函数检查文件大小和类型:
const handleFileChange = (file) => {
  // 文件大小检查
  const isLt50M = file.size / 1024 / 1024 < 50
  
  if (!isLt50M) {
    ElMessage.error('上传文件大小不能超过50MB!')
    return
  }
  
  selectedFile.value = file.raw
  // 其他处理...
}

6.2 后端异常处理
1) 使用自定义ServiceException处理业务异常
2) 统一Result响应格式，规范错误码和错误信息
3) 资源创建和更新时进行必要的验证

【ResourceService.java：异常处理示例】
资源创建方法中包含多处验证逻辑:
public void createResource(ResourceDTO resourceDTO, Long userId) {
    // 验证用户是否存在
    userService.getUserById(userId);
    
    // 验证分类是否存在
    ResourceCategory category = categoryMapper.selectById(resourceDTO.getCategoryId());
    if (category == null) {
        throw new ServiceException("资源分类不存在");
    }
    
    // 其他逻辑...
    
    // 插入数据库
    if (resourceMapper.insert(resource) <= 0) {
        throw new ServiceException("资源创建失败");
    }
}

七、与其他模块的交互

7.1 与用户模块交互
1) 获取资源上传者信息：查询用户表获取用户名等信息
2) 权限验证：验证用户登录状态和操作权限
3) 资源上传权限控制：根据用户角色控制资源上传能力

7.2 与报告模块交互
1) 资源举报功能：用户可以举报不合适的资源
2) 报告处理：管理员可以处理资源举报

八、性能优化措施

8.1 数据库优化
1) 资源表建立索引：user_id, category_id, status
2) 资源分类表建立索引：status

8.2 查询优化
1) 分页查询限制每页数量，避免大量数据查询
2) 使用MyBatis-Plus的复合查询，减少数据库访问次数

8.3 前端优化
1) 资源卡片使用懒加载技术
2) 图片资源使用缩略图，减少加载时间
3) 文件上传使用分片技术，支持大文件上传

九、总结

资源模块是师生交流论坛系统的重要组成部分，提供了教学资源的存储、管理和共享功能。通过分类管理和高效的搜索功能，用户可以快速找到所需资源。管理员拥有完整的资源管理权限，确保系统中的资源内容健康有序。资源上传和下载功能采用了安全的实现方式，保护用户隐私和系统安全。 