通知公告模块功能文档

一、模块字段设计

1.1 通知公告表(Notification)字段设计
id：整型，主键，通知ID，自增
title：字符串型，通知标题
content：字符串型，通知内容
user_id：整型，外键，关联用户表，发布用户ID
create_time：日期时间型，发布时间
status：整型，通知状态(0-下线，1-发布)

1.2 实体类定义

【Notification.java】
实体类包含通知公告表的基本字段，无扩展字段：
id：Long类型，通知ID，使用@TableId注解标识
title：String类型，通知标题，使用@NotBlank和@Size注解进行验证
content：String类型，通知内容，使用@NotBlank注解进行验证
userId：Long类型，发布用户ID
createTime：Date类型，发布时间
status：Integer类型，通知状态

1.3 数据传输对象定义

【NotificationCreateDTO.java】
title：String类型，通知标题，必填，长度2-200
content：String类型，通知内容，必填
status：Integer类型，通知状态，默认值为1(发布)

【NotificationUpdateDTO.java】
title：String类型，通知标题，可选，长度2-200
content：String类型，通知内容，可选
status：Integer类型，通知状态，可选

二、通知公告发布功能

2.1 功能概述
通知公告发布功能允许管理员创建和发布系统通知，通知发布后会在前台页面展示给所有用户。管理员可以设置通知的标题、内容和状态（发布或下线）。

2.2 前端实现流程

2.2.1 通知发布表单
1) 管理员点击"新增通知"按钮，打开表单对话框
2) 填写通知标题和内容
3) 选择通知状态（发布或下线）
4) 点击确定按钮提交

【NotificationManagement.vue：通知表单实现】
handleAdd函数实现了通知创建表单的初始化:
const handleAdd = () => {
  dialogTitle.value = '新增通知'
  notificationForm.id = ''
  notificationForm.title = ''
  notificationForm.content = ''
  notificationForm.status = 1
  dialogVisible.value = true
}

handleSubmit函数实现了表单提交功能:
const handleSubmit = async () => {
  if (!notificationFormRef.value) return

  try {
    await notificationFormRef.value.validate()
    
    if (notificationForm.id) {
      // 编辑
      request.put(`/notification/${notificationForm.id}`, notificationForm, {
        successMsg: '更新成功',
        errorMsg: '更新失败',
        onSuccess: () => {
          dialogVisible.value = false
          fetchNotifications()
        },
        onError: (error) => {
          console.error('更新通知失败:', error)
        }
      })
  } else {
      // 新增
      request.post('/notification/add', notificationForm, {
        successMsg: '创建成功',
        errorMsg: '创建失败',
        onSuccess: () => {
  dialogVisible.value = false
          fetchNotifications()
        },
        onError: (error) => {
          console.error('创建通知失败:', error)
        }
      })
    }
  } catch (error) {
    console.error('表单验证失败:', error)
  }
}

2.3 后端实现流程
1) 接收通知信息
2) 验证通知内容合法性
3) 设置发布人ID和发布时间
4) 将通知信息保存到数据库

【NotificationController.java：创建通知接口】
createNotification方法实现了通知创建功能:
@Operation(summary = "创建通知")
@PostMapping("/add")
public Result<?> createNotification(@RequestBody @Valid NotificationCreateDTO createDTO) {
    // 替换为实际从登录用户中获取的userId
    Long userId = 1L; // 这里假设为管理员ID
    Long notificationId = notificationService.createNotification(createDTO, userId);
    return Result.success("创建成功");
}

【NotificationService.java：创建通知方法】
createNotification方法进行属性拷贝并创建通知记录:
public Long createNotification(NotificationCreateDTO createDTO, Long userId) {
    Notification notification = new Notification();
    BeanUtils.copyProperties(createDTO, notification);
    
    notification.setUserId(userId);
    notification.setCreateTime(new Date());
    
    if (notificationMapper.insert(notification) <= 0) {
        throw new ServiceException("通知创建失败");
    }
    
    return notification.getId();
}

三、通知公告管理功能

3.1 功能概述
通知公告管理功能面向管理员，提供对系统通知的增删改查、状态控制和批量操作功能，确保系统通知的有效管理。

3.2 通知列表与搜索功能

3.2.1 前端实现流程
1) 管理员进入通知管理页面
2) 页面加载时获取通知列表数据
3) 提供搜索框和筛选条件（通知标题、通知状态）
4) 以表格形式展示通知列表，包含各类信息和操作按钮

【NotificationManagement.vue：通知列表实现】
fetchNotifications函数实现了通知列表的获取和搜索功能:
const fetchNotifications = () => {
  loading.value = true
  const params = {
    ...searchForm,
    currentPage: currentPage.value,
    size: pageSize.value
  }
  
  request.get('/notification/page', params, {
    showDefaultMsg: false,
    onSuccess: (res) => {
      tableData.value = res.records
      total.value = res.total
      loading.value = false
    },
    onError: (error) => {
      console.error('获取通知列表失败:', error)
    }
  })
}

3.2.2 后端实现流程
1) 接收查询参数（通知标题、通知状态）
2) 构建查询条件
3) 执行分页查询
4) 返回结果集

【NotificationService.java：分页查询方法】
getNotificationsByPage方法实现了通知的多条件分页查询:
public Page<Notification> getNotificationsByPage(String title, Integer status, Integer currentPage, Integer size) {
    LambdaQueryWrapper<Notification> queryWrapper = new LambdaQueryWrapper<>();
    
    // 添加查询条件
    if (StringUtils.isNotBlank(title)) {
        queryWrapper.like(Notification::getTitle, title);
    }
    
    if (status != null) {
        queryWrapper.eq(Notification::getStatus, status);
    }
    
    // 按创建时间降序排列
    queryWrapper.orderByDesc(Notification::getCreateTime);
    
    return notificationMapper.selectPage(new Page<>(currentPage, size), queryWrapper);
}

3.3 通知状态控制功能

3.3.1 前端实现流程
1) 通知列表中为每条通知提供状态开关
2) 点击开关切换通知状态（发布/下线）
3) 发送状态更新请求到后端

【NotificationManagement.vue：状态控制实现】
handleStatusChange函数实现了通知状态的切换功能:
const handleStatusChange = (id, status) => {
  request.put(`/notification/${id}/status?status=${status}`, null, {
    successMsg: '状态更新成功',
    errorMsg: '状态更新失败',
    onError: (error) => {
      console.error('修改状态失败:', error)
    }
  })
}

3.3.2 后端实现流程
1) 接收通知ID和状态参数
2) 更新通知状态

【NotificationService.java：状态更新方法】
updateNotificationStatus方法实现了通知状态的更新:
public void updateNotificationStatus(Long id, Integer status) {
    Notification notification = new Notification();
    notification.setId(id);
    notification.setStatus(status);
    
    if (notificationMapper.updateById(notification) <= 0) {
        throw new ServiceException("状态更新失败");
    }
}

3.4 通知编辑功能

3.4.1 前端实现流程
1) 通知列表中为每条通知提供编辑按钮
2) 点击编辑按钮，打开编辑表单，并填充现有通知数据
3) 修改通知信息后提交更新

【NotificationManagement.vue：编辑功能实现】
handleEdit函数实现了通知编辑功能:
const handleEdit = (row) => {
  dialogTitle.value = '编辑通知'
  Object.assign(notificationForm, row)
  dialogVisible.value = true
}

3.4.2 后端实现流程
1) 接收通知ID和更新数据
2) 验证通知存在性
3) 更新通知信息

【NotificationService.java：更新方法】
updateNotification方法实现了通知的更新:
public void updateNotification(Long id, NotificationUpdateDTO updateDTO) {
    Notification notification = notificationMapper.selectById(id);
    if (notification == null) {
        throw new ServiceException("通知不存在");
    }

    if (StringUtils.isNotBlank(updateDTO.getTitle())) {
        notification.setTitle(updateDTO.getTitle());
    }
    
    if (StringUtils.isNotBlank(updateDTO.getContent())) {
        notification.setContent(updateDTO.getContent());
    }
    
    if (updateDTO.getStatus() != null) {
        notification.setStatus(updateDTO.getStatus());
    }
    
    if (notificationMapper.updateById(notification) <= 0) {
        throw new ServiceException("通知更新失败");
    }
}

3.5 通知删除功能

3.5.1 前端实现流程
1) 单条删除：点击删除按钮，确认后删除单条通知
2) 批量删除：选择多条通知记录，点击批量删除，确认后删除选中的记录

【NotificationManagement.vue：删除功能实现】
handleDelete函数实现了单条通知的删除:
const handleDelete = async (row) => {
  try {
    await ElMessageBox.confirm('确认删除该通知吗？', '提示', {
      type: 'warning'
    })
    
    request.delete(`/notification/${row.id}`, {
      successMsg: '删除成功',
      errorMsg: '删除失败',
      onSuccess: () => {
        fetchNotifications()
      },
      onError: (error) => {
        console.error('删除通知失败:', error)
      }
    })
  } catch (error) {
    // 用户取消删除，不做处理
  }
}

handleBatchDelete函数实现了批量删除功能:
const handleBatchDelete = async () => {
  if (selectedRows.value.length === 0) return
  
  try {
    await ElMessageBox.confirm(`确认删除选中的 ${selectedRows.value.length} 个通知吗？`, '提示', {
      type: 'warning'
    })
    
    const ids = selectedRows.value.map(row => row.id)
    request.delete('/notification/batch', { 
      data: ids 
    }, {
      successMsg: '批量删除成功',
      errorMsg: '批量删除失败',
      onSuccess: () => {
        fetchNotifications()
      },
      onError: (error) => {
        console.error('批量删除失败:', error)
      }
    })
  } catch (error) {
    // 用户取消删除，不做处理
  }
}

3.5.2 后端实现流程
1) 单条删除：接收通知ID，删除对应记录
2) 批量删除：接收通知ID列表，批量删除记录

【NotificationService.java：删除方法】
deleteNotification和batchDeleteNotifications方法实现了单条和批量删除:
public void deleteNotification(Long id) {
    if (notificationMapper.deleteById(id) <= 0) {
        throw new ServiceException("通知删除失败");
    }
}

public void batchDeleteNotifications(List<Long> ids) {
    if (ids == null || ids.isEmpty()) {
        throw new ServiceException("请选择要删除的通知");
    }
    
    int count = notificationMapper.deleteBatchIds(ids);
    if (count <= 0) {
        throw new ServiceException("批量删除失败");
    }
}

四、前台通知公告展示功能

4.1 功能概述
前台通知公告展示功能面向普通用户，提供系统通知的浏览功能，通过不同的展示方式（通知面板、通知列表页、首页轮播等）让用户及时了解系统公告。

4.2 通知列表页面

4.2.1 实现流程
1) 用户进入通知公告页面
2) 页面加载时获取已发布的通知列表
3) 以列表形式展示通知标题、发布时间和摘要
4) 支持点击标题查看通知详情
5) 提供搜索和分页功能

【Notification.vue：通知列表页实现】
fetchNotifications函数实现了已发布通知的获取:
const fetchNotifications = async () => {
  loading.value = true
  try {
    const params = {
      title: searchKeyword.value,
      status: 1, // 只获取已发布的通知
      currentPage: currentPage.value,
      size: pageSize.value
    }

    await request.get('/notification/page', params, {
      showDefaultMsg: false,
      onSuccess: (data) => {
        notifications.value = data.records || []
        total.value = data.total || 0
      }
    })
  } catch (error) {
    console.error('获取通知列表失败:', error)
    ElMessage.error('获取通知列表失败')
  } finally {
    loading.value = false
  }
}

4.3 通知详情展示

4.3.1 实现流程
1) 用户点击通知标题
2) 弹出通知详情对话框
3) 显示通知完整内容和发布时间

【Notification.vue：通知详情实现】
showNotificationDetail函数和详情对话框实现了通知详情展示:
const showNotificationDetail = (notification) => {
  currentNotification.value = notification
  detailDialogVisible.value = true
}

<!-- 通知详情对话框 -->
<el-dialog
  v-model="detailDialogVisible"
  :title="currentNotification?.title"
  width="600px"
>
  <div class="notification-detail" v-if="currentNotification">
    <div class="detail-meta">
      <span class="publish-time">发布时间：{{ formatDate(currentNotification.createTime) }}</span>
    </div>
    <div class="detail-content" v-html="currentNotification.content"></div>
  </div>
</el-dialog>

4.4 通知面板组件

4.4.1 实现流程
1) 通知面板组件加载时获取最新的系统通知
2) 以简洁的列表形式展示通知标题和发布时间
3) 支持点击查看通知详情
4) 提供"查看全部"链接跳转到通知列表页面

【NotificationPanel.vue：通知面板实现】
fetchNotifications函数实现了通知数据的获取:
const fetchNotifications = () => {
  loading.value = true
  
  request.get('/notification/published', {
    showDefaultMsg: false,
    onSuccess: (data) => {
      notifications.value = data
    },
    onError: (error) => {
      console.error('获取通知失败:', error)
    },
    onComplete: () => {
      loading.value = false
    }
  })
}

通知面板的布局和交互:
<div class="notification-panel">
  <div class="panel-header">
    <h3 class="panel-title">
      <el-icon><Bell /></el-icon>
      系统通知
    </h3>
    <el-button v-if="notifications.length > 0" text @click="showAllNotifications">查看全部</el-button>
  </div>

  <!-- 通知列表 -->
  <div v-else class="notification-list">
    <div
      v-for="item in notifications.slice(0, displayCount)"
      :key="item.id"
      class="notification-item"
      @click="handleViewNotification(item)"
    >
      <div class="notification-content">
        <div class="notification-title">{{ item.title }}</div>
        <div class="notification-time">{{ formatDate(item.createTime) }}</div>
      </div>
      <el-icon><ArrowRight /></el-icon>
    </div>
  </div>
</div>

4.5 首页系统通知展示

4.5.1 实现流程
1) 首页加载时获取最新的系统通知
2) 在首页合适位置展示通知列表
3) 允许用户点击查看通知详情或进入通知列表页面

【Home.vue：首页通知展示】
在获取首页数据时一并获取系统通知:
// 获取系统通知
await request.get(
  "/notification/page",
  {
  status: 1, 
  currentPage: 1, 
    size: 3,
  },
  {
  showDefaultMsg: false,
  onSuccess: (data) => {
      const notices = data.records || [];
      notifications.value = notices.map((notice) => ({
      id: notice.id || notice.notification_id,
      title: notice.title,
      content: notice.content,
        createTime: notice.createTime || notice.create_time,
      }));
    },
  }
)

五、后端API接口设计

5.1 通知管理接口

5.1.1 获取通知列表接口
【NotificationController.java：分页查询接口】
@Operation(summary = "分页查询通知")
@GetMapping("/page")
public Result<?> getNotificationsByPage(
        @RequestParam(required = false) String title,
        @RequestParam(required = false) Integer status,
        @RequestParam(defaultValue = "1") Integer currentPage,
        @RequestParam(defaultValue = "10") Integer size) {
    Page<Notification> page = notificationService.getNotificationsByPage(title, status, currentPage, size);
    return Result.success(page);
}

5.1.2 获取已发布通知接口
【NotificationController.java：获取已发布通知接口】
@Operation(summary = "获取所有已发布通知")
@GetMapping("/published")
public Result<?> getPublishedNotifications() {
    List<Notification> notifications = notificationService.getPublishedNotifications();
    return Result.success(notifications);
}

5.1.3 创建通知接口
【NotificationController.java：创建通知接口】
@Operation(summary = "创建通知")
@PostMapping("/add")
public Result<?> createNotification(@RequestBody @Valid NotificationCreateDTO createDTO) {
    // 替换为实际从登录用户中获取的userId
    Long userId = 1L; // 这里假设为管理员ID
    Long notificationId = notificationService.createNotification(createDTO, userId);
    return Result.success("创建成功");
}

5.1.4 更新通知接口
【NotificationController.java：更新通知接口】
@Operation(summary = "更新通知信息")
@PutMapping("/{id}")
public Result<?> updateNotification(@PathVariable Long id, @RequestBody NotificationUpdateDTO updateDTO) {
    notificationService.updateNotification(id, updateDTO);
    return Result.success("更新成功");
}

5.1.5 删除通知接口
【NotificationController.java：删除通知接口】
@Operation(summary = "根据id删除通知")
@DeleteMapping("/{id}")
public Result<?> deleteNotification(@PathVariable Long id) {
    notificationService.deleteNotification(id);
    return Result.success("删除成功");
}

六、异常处理机制

6.1 前端异常处理
1) 使用try-catch捕获异常，并在console中记录错误信息
2) 使用ElMessage显示友好的错误提示
3) 表单提交前进行数据验证
4) 操作失败时提供明确的错误信息

【NotificationManagement.vue：异常处理示例】
handleSubmit函数包含表单验证和错误处理:
try {
  await notificationFormRef.value.validate()
  // 提交表单...
} catch (error) {
  console.error('表单验证失败:', error)
}

6.2 后端异常处理
1) 使用自定义ServiceException处理业务异常
2) 统一Result响应格式，规范错误码和错误信息
3) 通知创建和更新时进行必要的验证
4) 确保数据操作成功，否则抛出异常

【NotificationService.java：异常处理示例】
updateNotification方法中的异常处理:
public void updateNotification(Long id, NotificationUpdateDTO updateDTO) {
    Notification notification = notificationMapper.selectById(id);
    if (notification == null) {
        throw new ServiceException("通知不存在");
    }
    
    // 更新属性...
    
    if (notificationMapper.updateById(notification) <= 0) {
        throw new ServiceException("通知更新失败");
    }
}

七、优化与性能提升

7.1 数据库优化
1) 通知表创建索引：title, status, create_time
2) 使用MyBatis-Plus提供的分页和条件查询，减少数据库访问

7.2 前端优化
1) 通知内容的懒加载处理
2) 长列表虚拟滚动
3) 通知详情按需加载
4) 使用防抖处理搜索输入

【Notification.vue：前端优化示例】
通知内容截断处理，避免过长内容影响性能:
const truncateContent = (content) => {
  if (!content) return ''
  // 移除HTML标签
  const plainText = content.replace(/<[^>]+>/g, '')
  // 移除特殊字符和多余空格
  const cleanText = plainText
    .replace(/&[^;]+;/g, '')
    .replace(/\s+/g, ' ')
    .trim()
  return cleanText.length > 100 ? cleanText.substring(0, 100) + '...' : cleanText
}

八、总结

通知公告模块是师生交流论坛系统的重要组成部分，提供了系统管理员发布公告、用户浏览公告的功能。通过通知公告功能，管理员可以向全体用户传达重要信息，如系统更新、活动通知、规则变更等。

模块设计合理，既提供了后台管理功能，支持管理员进行通知的增删改查和状态控制，又在前台提供了多种通知展示方式，如通知列表页、通知面板组件和首页通知展示，确保用户能够及时了解系统公告信息。通知公告内容支持富文本，可以包含格式化文本、链接等元素，提升了信息传达的丰富性。

通过通知公告模块，实现了师生交流论坛系统中重要信息的有效传达，提升了系统的信息化管理水平和用户体验。 